# Create an array type specification for structured extraction

Create an array type specification for structured extraction

## Usage

``` r
create_array_type(description = "The extracted array of values")
```
