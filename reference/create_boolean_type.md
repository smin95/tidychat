# Create a boolean type specification for structured extraction

Create a boolean type specification for structured extraction

## Usage

``` r
create_boolean_type(description = "The extracted boolean value")
```
